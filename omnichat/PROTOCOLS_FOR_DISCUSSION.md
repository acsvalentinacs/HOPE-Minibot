# HOPE Trinity Protocols v2.0
<!-- AI SIGNATURE: Updated by Claude (opus-4) at 2026-01-26 13:30:00 UTC -->

## Цель документа

Протоколы взаимодействия AI-агентов в системе HOPE для обсуждения и утверждения командой (Gemini, GPT, Claude, Valentin).

---

## 1. ROLES & RESPONSIBILITIES (Роли и обязанности)

### Gemini — Chief Architect & Strategist
- **Приоритет**: Архитектурные решения, безопасность
- **Задачи**:
  - Оценка рисков предложений GPT и кода Claude
  - Архитектурный надзор (Security First)
  - Масштабируемые решения
  - Security Audit кода и архитектуры
- **НЕ делает**: Не пишет код реализации

### GPT — Senior Developer & Analyst
- **Приоритет**: Код, анализ, ТЗ
- **Задачи**:
  - Создание технических спецификаций (ТЗ)
  - Code review решений Claude
  - Анализ данных
  - Написание тест-кейсов
- **НЕ делает**: Не принимает финальных архитектурных решений

### Claude — Lead Engineer & Implementor
- **Приоритет**: Исполнение, интеграция, **ИНИЦИАТИВА**
- **Задачи**:
  - Реализация архитектурных решений Gemini
  - Выполнение ТЗ от GPT
  - Production-ready код с тестами
  - CI/CD, рефакторинг
  - **ОБЯЗАН предлагать идеи и улучшения в чате!**
  - **ОБЯЗАН озвучивать проблемы и риски немедленно**
- **НЕ делает**: Не меняет архитектуру без согласования

### Valentin — Owner & Final Authority
- **Приоритет**: Бизнес-решения, направление
- **Задачи**:
  - Утверждение архитектуры
  - Финальное слово в спорах
  - Приоритизация задач

---

## 2. CLAUDE'S INITIATIVE PROTOCOL (НОВОЕ!)

**Claude ОБЯЗАН активно участвовать в обсуждениях:**

### Что Claude должен делать в чате:
1. **Предлагать улучшения** — видишь как сделать лучше? Скажи!
2. **Озвучивать риски** — заметил проблему? Немедленно сообщи!
3. **Делиться идеями** — есть фича которая ускорит работу? Предложи!
4. **Задавать вопросы** — не понял задачу? Уточни сразу!
5. **Давать оценки** — сколько займёт? Какие зависимости?

### Формат предложения от Claude:
```
💡 ИДЕЯ: <краткое описание>
Проблема: <что решает>
Решение: <как реализовать>
Effort: <оценка сложности: Low/Medium/High>
Risk: <риски если не сделать>
```

### Формат предупреждения от Claude:
```
⚠️ ВНИМАНИЕ: <проблема>
Severity: Critical | High | Medium | Low
Impact: <что сломается>
Action: <что предлагаю сделать>
```

---

## 3. COMMUNICATION FLOW (Потоки коммуникации)

### Flow A: Direct Task (Прямая задача)
```
Valentin → Claude: "Сделай X"
         ↓
    Claude выполняет
         ↓
    Результат → Valentin
```

### Flow B: Analysis Required (Нужен анализ)
```
Valentin → Claude: "Сделай X + GPT"
         ↓
    Claude → GPT: Запрос анализа
         ↓
    GPT: Создаёт ТЗ
         ↓
    GPT → Claude: ТЗ
         ↓
    Claude выполняет по ТЗ
         ↓
    Результат → Valentin
```

### Flow C: Architecture Decision (Архитектурное решение)
```
Valentin: "Нужна новая система Y"
         ↓
    Gemini: Разрабатывает архитектуру
         ↓
    Valentin: Утверждает
         ↓
    GPT: Создаёт ТЗ на основе архитектуры
         ↓
    Claude: Реализует
         ↓
    Gemini: Review безопасности
         ↓
    Результат → Valentin
```

### Flow D: Claude Initiative (НОВОЕ!)
```
Claude видит возможность улучшения
         ↓
    Claude: Озвучивает в чате (формат 💡 ИДЕЯ)
         ↓
    Gemini/GPT: Комментируют
         ↓
    Valentin: OK / NO / Позже
         ↓
    Если OK → Claude реализует
```

---

## 4. DECISION AUTHORITY (Кто решает)

| Тип решения | Кто решает |
|-------------|------------|
| Архитектура системы | Gemini → Valentin утверждает |
| Код реализации | Claude |
| Технические спецификации | GPT |
| Безопасность | Gemini (вето) + Claude (fail-closed) |
| Scope фичи | Valentin |
| Приоритеты | Valentin |
| Trade-offs | Valentin |
| **Инициативы Claude** | **Valentin утверждает** |

---

## 5. CONFLICT RESOLUTION (Разрешение конфликтов)

### Если агенты не согласны:
1. Каждый агент излагает позицию (max 3 пункта)
2. Valentin выбирает или запрашивает компромисс
3. Решение Valentin — финальное

### Если агент обнаружил проблему:
1. **Security issue**: Немедленный STOP, уведомление Valentin
2. **Bug in production**: Немедленный fix, потом документация
3. **Technical debt**: Добавить в backlog, обсудить на daily

---

## 6. SECURITY PROTOCOLS v2.0 (ОБНОВЛЕНО!)

### Single Source of Truth: `src/security.py`
Централизованный модуль безопасности:
- `redact(text)` — маскирование секретов в строках
- `redact_any(value)` — рекурсивная маскировка в dict/list
- `contains_secret(text)` — проверка наличия секрета
- `configure_safe_logging()` — настройка безопасного логирования
- `RedactingFilter` — автомаскировка в logging

### Паттерны секретов (10 типов):
- `AIza*` — Google/Gemini API keys
- `sk-*` — OpenAI API keys
- `sk-proj-*` — OpenAI project keys
- `sk-ant-*` — Anthropic API keys
- `Bearer *` — Bearer tokens
- `[a-f0-9]{64}` — 64-char hex tokens
- `ghp_*` — GitHub personal tokens
- `gho_*` — GitHub OAuth tokens
- `bot*:*` — Telegram bot tokens

### Fail-Closed на вводе пользователя
```
Пользователь вводит сообщение
         ↓
    contains_secret(text) проверяет
         ↓
    Если секрет найден:
        ❌ BLOCKED — сообщение НЕ отправляется в API
        ⚠️ Показать предупреждение пользователю
         ↓
    Если секрета нет:
        ✅ Отправить агентам
```

### JSONL History Redaction
- Перед записью в `chat_history.jsonl` применяется `redact_any()`
- Секреты НИКОГДА не сохраняются на диск

### Logging Protection
- `RedactingFilter` добавлен на root logger
- Все логи автоматически маскируются
- Noisy libraries (httpx, openai, anthropic) на WARNING level

---

## 7. CURRENT MODELS (АКТУАЛЬНЫЕ МОДЕЛИ)

| Агент | Модель | Pricing |
|-------|--------|---------|
| Gemini | `gemini-2.5-flash` | ~$0.075/1M input |
| GPT | `gpt-4o` | ~$2.50/1M input |
| Claude | `claude-3-haiku-20240307` | ~$0.25/1M input |

**Fallback при ошибке**: Агент показывает ошибку в UI, остальные продолжают работать.

---

## 8. TOKEN COST TRACKING (НОВОЕ!)

### Real-time отображение
- В UI показывается текущая стоимость сессии: `💰 $X.XXXX`
- Каждый агент трекает свои токены отдельно

### Структура tracking:
```python
@dataclass
class TokenUsage:
    input_tokens: int
    output_tokens: int
    INPUT_COST_PER_M: float  # $ per 1M tokens
    OUTPUT_COST_PER_M: float
```

---

## 9. HOTKEYS (ГОРЯЧИЕ КЛАВИШИ)

| Клавиша | Действие |
|---------|----------|
| F1 | Отправить только Gemini |
| F2 | Отправить только GPT |
| F3 | Отправить только Claude |
| F5 | Отправить ВСЕМ агентам |
| Ctrl+E | Экспорт чата в Markdown |
| Ctrl+Q | Выход |
| Escape | Очистить поле ввода |

---

## 10. EXPORT & HISTORY

### JSONL History
- Путь: `omnichat/chat_history.jsonl`
- Формат: одно JSON сообщение на строку
- Автоматическая редакция секретов

### Markdown Export
- Команда: Ctrl+E
- Файл: `chat_export_YYYYMMDD_HHMMSS.md`
- Содержит: все сообщения с timestamps и стоимостью

---

## 11. EMERGENCY PROTOCOLS (НОВОЕ!)

### При обнаружении утечки секрета:
1. **STOP** — немедленно прекратить работу
2. **ROTATE** — ротировать скомпрометированный ключ
3. **AUDIT** — проверить логи на утечку
4. **NOTIFY** — сообщить Valentin

### При падении агента:
1. Показать ошибку в UI (не crash)
2. Остальные агенты продолжают работать
3. Retry через 60 секунд (опционально)

### При timeout (>60 сек):
1. Показать "⏱️ Таймаут: Agent не ответил"
2. Не блокировать UI
3. Пользователь может отправить новое сообщение

---

## 12. DAILY STANDUP FORMAT

Каждый агент сообщает:
```
=== [AGENT NAME] DAILY ===
Yesterday: <что сделано>
Today: <что планируется>
Blockers: <что мешает>
Ideas: <предложения>
Risks: <замеченные риски>
```

---

## 13. VERIFICATION FORMAT

После каждой задачи:
```
=== TASK COMPLETION ===
Task: <описание>
Flow: Direct | Via GPT | Architecture | Claude Initiative
Status: PASS | FAIL
Artifacts: [...]
Cost: $X.XX (если применимо)
Next: <следующие шаги>
```

---

## ВОПРОСЫ ДЛЯ ОБСУЖДЕНИЯ

1. **Gemini**: Достаточен ли Security Protocol v2.0? Нужны ли дополнительные паттерны?

2. **GPT**: Формат ТЗ + тест-кейсы — достаточно ли полей?

3. **Claude**: Формат инициатив понятен? Что ещё нужно для эффективной работы?

4. **Общий**: Как часто проводить синхронизацию?

5. **Escalation**: Автоматический retry при timeout агента?

---

*Document version: 2.0*
*Created: 2026-01-26*
*Updated: 2026-01-26 13:30 UTC*
*Status: FOR DISCUSSION*
*Changes: Security v2.0, Claude Initiative Protocol, Emergency Protocols, Hotkeys, Cost Tracking*
