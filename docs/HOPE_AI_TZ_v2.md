# HOPE AI ECOSYSTEM v2.0 - ТЕХНИЧЕСКОЕ ЗАДАНИЕ

**Версия:** 2.0
**Дата:** 2026-01-31
**Автор:** Claude + Валентин
**Статус:** PRODUCTION READY

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### ✅ УСТАНОВЛЕНО И РАБОТАЕТ:

| Компонент | Файл | Статус | Данные |
|-----------|------|--------|--------|
| Pump Detector | `scripts/pump_detector.py` | ✅ ACTIVE | WebSocket Binance → metrics |
| AutoTrader | `scripts/autotrader.py` | ✅ ACTIVE | Signals → Orders |
| Eye of God V3 | `scripts/eye_of_god_v3.py` | ⚠️ EMBEDDED | Two-chamber decisions |
| Position Watchdog | `scripts/position_watchdog.py` | ✅ ACTIVE | Protect positions |
| Pricefeed Bridge | `scripts/pricefeed_bridge.py` | ✅ ACTIVE | 34 symbols/5s |
| Anomaly Model | `state/ai/anomaly_model.joblib` | ✅ TRAINED | 1.3 MB |
| Signal Classifier | `state/ai/signal_classifier.joblib` | ✅ TRAINED | 74 KB |

### ❌ НЕ РАБОТАЕТ / НЕ ИНТЕГРИРОВАНО:

| Компонент | Файл | Проблема | Критичность |
|-----------|------|----------|-------------|
| AI Predictor V2 | `scripts/ai_predictor_v2.py` | asyncio crash | 🔴 HIGH |
| Signal Aggregator | `scripts/signal_aggregator.py` | No daemon mode | 🟡 MEDIUM |
| Live Learning | `scripts/live_learning.py` | Not running | 🔴 HIGH |
| Adaptive Target AI | `scripts/adaptive_target_ai.py` | Not integrated | 🟡 MEDIUM |
| Pump Predictor | `scripts/pump_predictor.py` | Not running | 🟡 MEDIUM |
| Eye Trainer | `scripts/eye_trainer.py` | Manual only | 🟡 MEDIUM |
| Morning Scan | `scripts/morning_scan.py` | Not scheduled | 🟢 LOW |

---

## 🏗️ ЦЕЛЕВАЯ АРХИТЕКТУРА

```
┌─────────────────────────────────────────────────────────────────────┐
│                         HOPE AI ECOSYSTEM v2.0                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐          │
│  │   SOURCES    │    │  PROCESSING  │    │   EXECUTION  │          │
│  ├──────────────┤    ├──────────────┤    ├──────────────┤          │
│  │ MoonBot TG   │───▶│ Pump Detector│───▶│ AutoTrader   │          │
│  │ Binance WS   │    │ + AI Filter  │    │ + Eye of God │          │
│  │ TradingView  │    │              │    │              │          │
│  └──────────────┘    └──────┬───────┘    └──────┬───────┘          │
│                             │                    │                   │
│                             ▼                    ▼                   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    AI BRAIN LAYER                             │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │                                                               │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐              │   │
│  │  │ Predictor  │  │ Aggregator │  │  Adaptive  │              │   │
│  │  │    V2      │  │            │  │  Target AI │              │   │
│  │  │            │  │ Combines   │  │            │              │   │
│  │  │ ML Model   │  │ All Signals│  │ Dynamic TP │              │   │
│  │  │ Confidence │  │ Score 0-1  │  │ Based on   │              │   │
│  │  │ 0.0 - 1.0  │  │            │  │ Volatility │              │   │
│  │  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘              │   │
│  │        │               │               │                      │   │
│  │        └───────────────┼───────────────┘                      │   │
│  │                        ▼                                      │   │
│  │              ┌──────────────────┐                             │   │
│  │              │  DECISION ENGINE │                             │   │
│  │              │  (Eye of God V3) │                             │   │
│  │              │                  │                             │   │
│  │              │  confidence > 0.6│                             │   │
│  │              │  R:R >= 3:1      │                             │   │
│  │              │  liquidity > $5M │                             │   │
│  │              └────────┬─────────┘                             │   │
│  │                       │                                       │   │
│  └───────────────────────┼───────────────────────────────────────┘   │
│                          ▼                                           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    LEARNING LAYER                             │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │                                                               │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐              │   │
│  │  │   Live     │  │   Eye      │  │   Regime   │              │   │
│  │  │  Learning  │  │  Trainer   │  │  Detector  │              │   │
│  │  │            │  │            │  │            │              │   │
│  │  │ Outcomes   │  │ Retrain    │  │ Bull/Bear  │              │   │
│  │  │ → Model    │  │ Every 100  │  │ Sideways   │              │   │
│  │  │ Update     │  │ Trades     │  │ Detection  │              │   │
│  │  └────────────┘  └────────────┘  └────────────┘              │   │
│  │                                                               │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    MONITORING LAYER                           │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │  Dashboard :8080 │ Watchdog │ Circuit Breaker │ Event Ledger │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 ПОТОКИ ДАННЫХ

### 1. SIGNAL FLOW (Сигналы)

```
Binance WebSocket
       │
       ▼
┌──────────────────┐
│  pump_detector   │ ──▶ state/ai/signals/raw.jsonl
│  (port: none)    │
└────────┬─────────┘
         │ HTTP POST /signal
         ▼
┌──────────────────┐
│   autotrader     │ ──▶ state/ai/autotrader/decisions.jsonl
│  (port: 8200)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Binance API     │ ──▶ state/ai/trades/executed.jsonl
│  (MARKET orders) │
└──────────────────┘
```

### 2. PREDICTION FLOW (AI Предсказания)

```
┌──────────────────┐
│  ai_predictor_v2 │ ◀── state/ai/training_data.jsonl
│  (port: 8300)    │
└────────┬─────────┘
         │ Reads
         ▼
┌──────────────────┐
│ signal_classifier│ ◀── ML Model (joblib)
│     .joblib      │
└────────┬─────────┘
         │ Returns
         ▼
┌──────────────────┐
│  confidence      │ ──▶ 0.0 - 1.0 score
│  + probability   │
└──────────────────┘
```

### 3. LEARNING FLOW (Обучение)

```
┌──────────────────┐
│ Trade Outcomes   │ ◀── Binance fills + PnL
│ (TP/SL/TIMEOUT)  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  live_learning   │ ──▶ state/ai/outcomes/history.jsonl
│                  │
└────────┬─────────┘
         │ Every 100 trades
         ▼
┌──────────────────┐
│  eye_trainer     │ ──▶ Retrain models
│                  │     signal_classifier.joblib
└────────┬─────────┘     anomaly_model.joblib
         │
         ▼
┌──────────────────┐
│  Models Updated  │ ──▶ Hot-reload in production
└──────────────────┘
```

---

## 📁 ФАЙЛОВАЯ СТРУКТУРА

```
minibot/
├── scripts/                    # Исполняемые компоненты
│   ├── autotrader.py          # 🟢 CORE: Основной трейдер
│   ├── pump_detector.py       # 🟢 CORE: Детектор пампов
│   ├── position_watchdog.py   # 🟢 CORE: Защита позиций
│   ├── pricefeed_bridge.py    # 🟢 CORE: Цены (port 8100)
│   ├── eye_of_god_v3.py       # 🟡 AI: Двухкамерные решения
│   ├── ai_predictor_v2.py     # 🔴 AI: ML предсказания (BROKEN)
│   ├── adaptive_target_ai.py  # 🟡 AI: Динамические TP/SL
│   ├── signal_aggregator.py   # 🟡 AI: Агрегация сигналов
│   ├── live_learning.py       # 🔴 AI: Онлайн обучение (NOT RUNNING)
│   ├── eye_trainer.py         # 🟡 AI: Переобучение моделей
│   ├── pump_predictor.py      # 🟡 AI: Предсказание пампов
│   └── morning_scan.py        # 🟢 UTIL: Утренний скан
│
├── core/                       # Библиотеки
│   ├── pretrade_pipeline.py   # 7 Guards (cannot bypass)
│   ├── risk_manager_v1.py     # Risk management
│   ├── event_ledger.py        # Correlation IDs
│   └── ai/
│       └── ml_predictor.py    # ML inference
│
├── state/                      # Состояние системы
│   └── ai/
│       ├── signals/           # Входящие сигналы
│       ├── autotrader/        # Решения трейдера
│       ├── outcomes/          # Результаты сделок
│       ├── learning/          # Данные обучения
│       ├── models/            # Обученные модели
│       ├── anomaly_model.joblib
│       ├── signal_classifier.joblib
│       ├── training_data.jsonl
│       ├── decisions.jsonl
│       └── regime.jsonl
│
└── logs/                       # Логи
    ├── autotrader.log
    ├── pump_detector.log
    ├── position_watchdog.log
    └── ai_predictor.log
```

---

## 🔧 ПЛАН УСТАНОВКИ

### ФАЗА 1: ИСПРАВЛЕНИЕ (День 1, 2 часа)

#### 1.1 Починить AI Predictor V2

**Проблема:** asyncio crash при запуске
**Решение:** Исправить async/await pattern

```python
# Было (падает):
asyncio.run(test())

# Надо (работает):
if __name__ == "__main__":
    import sys
    if len(sys.argv) > 1 and sys.argv[1] == "--daemon":
        asyncio.run(main_daemon())
    else:
        asyncio.run(test())
```

**Команда проверки:**
```bash
python scripts/ai_predictor_v2.py --daemon
```

#### 1.2 Добавить daemon mode в Signal Aggregator

```python
# Добавить в signal_aggregator.py:
async def daemon_loop():
    while True:
        await aggregate_signals()
        await asyncio.sleep(10)

if __name__ == "__main__":
    parser.add_argument("--daemon", action="store_true")
    if args.daemon:
        asyncio.run(daemon_loop())
```

### ФАЗА 2: ИНТЕГРАЦИЯ (День 1-2, 4 часа)

#### 2.1 Подключить Live Learning

**Задача:** Автоматически обновлять модели на основе результатов

```
Trigger: Trade outcome (TP_HIT / SL_HIT)
    │
    ▼
live_learning.py
    │
    ├─▶ Записать в outcomes/history.jsonl
    │
    ├─▶ Если trades >= 100:
    │       └─▶ Запустить eye_trainer.py
    │
    └─▶ Hot-reload models
```

**Интеграция в autotrader.py:**
```python
from scripts.live_learning import record_outcome

async def on_trade_close(trade_result):
    await record_outcome(trade_result)
```

#### 2.2 Подключить Adaptive Target AI

**Задача:** Динамические TP/SL на основе волатильности

```python
# В autotrader.py перед открытием позиции:
from scripts.adaptive_target_ai import calculate_targets

targets = await calculate_targets(
    symbol=signal.symbol,
    entry_price=current_price,
    volatility=atr_14,
    regime=market_regime
)
# targets = {"tp": 0.03, "sl": 0.01, "rr": 3.0}
```

### ФАЗА 3: ОБУЧЕНИЕ (День 2-3, постоянно)

#### 3.1 Начальное обучение

```bash
# 1. Собрать исторические данные
python scripts/sync_training_data.py

# 2. Обучить Signal Classifier
python scripts/eye_trainer.py --train --data state/ai/training_data.jsonl

# 3. Обучить Anomaly Detector
python scripts/eye_trainer.py --train-anomaly --data state/ai/anomaly.jsonl

# 4. Валидация
python scripts/eye_trainer.py --validate
```

#### 3.2 Онлайн обучение (Live Learning)

```
Каждые 100 сделок:
    1. Собрать outcomes
    2. Добавить в training_data.jsonl
    3. Retrain models (incremental)
    4. Hot-reload
    5. Log metrics
```

### ФАЗА 4: МОНИТОРИНГ (День 3)

#### 4.1 Dashboard на :8080

```
┌─────────────────────────────────────────────────────────┐
│                    HOPE AI DASHBOARD                     │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  [REFRESH ALL]  [START STACK]  [STOP ALL]               │
│                                                          │
│  ┌────────────────┐  ┌────────────────┐                 │
│  │ Win Rate       │  │ Total PnL      │                 │
│  │   ████████ 62% │  │   +$47.50      │                 │
│  │   [REFRESH]    │  │   [REFRESH]    │                 │
│  └────────────────┘  └────────────────┘                 │
│                                                          │
│  ┌────────────────┐  ┌────────────────┐                 │
│  │ Model Accuracy │  │ Trades Today   │                 │
│  │   ████████ 71% │  │      23        │                 │
│  │   [REFRESH]    │  │   [REFRESH]    │                 │
│  └────────────────┘  └────────────────┘                 │
│                                                          │
│  ═══════════════════════════════════════════════════    │
│                     GRAPHS                               │
│  ═══════════════════════════════════════════════════    │
│                                                          │
│  1. PnL Over Time         [REFRESH]                     │
│     ┌──────────────────────────────┐                    │
│     │    ╱╲    ╱╲                  │                    │
│     │   ╱  ╲  ╱  ╲    ╱╲          │                    │
│     │──╱────╲╱────╲──╱──╲─────────│                    │
│     │              ╲╱              │                    │
│     └──────────────────────────────┘                    │
│                                                          │
│  2. Win Rate Trend        [REFRESH]                     │
│     ┌──────────────────────────────┐                    │
│     │████████████████████████      │                    │
│     │60% ───────────────────────── │                    │
│     └──────────────────────────────┘                    │
│                                                          │
│  3. AI Confidence Distribution  [REFRESH]               │
│     ┌──────────────────────────────┐                    │
│     │        ▄▄▄▄                  │                    │
│     │      ▄█████▄                 │                    │
│     │    ▄█████████▄               │                    │
│     │  ▄█████████████▄             │                    │
│     │0.0    0.5    1.0             │                    │
│     └──────────────────────────────┘                    │
│                                                          │
│  4. Model Performance     [REFRESH]                     │
│     ┌──────────────────────────────┐                    │
│     │ Accuracy:  71% ████████      │                    │
│     │ Precision: 68% ███████       │                    │
│     │ Recall:    74% ████████      │                    │
│     │ F1-Score:  71% ████████      │                    │
│     └──────────────────────────────┘                    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 ДАННЫЕ ДЛЯ ОБУЧЕНИЯ

### Источники данных:

| Источник | Файл | Формат | Размер |
|----------|------|--------|--------|
| Trade history | `training_data.jsonl` | JSONL | 76 KB |
| Anomalies | `anomaly.jsonl` | JSONL | 10 MB |
| Decisions | `decisions.jsonl` | JSONL | 167 KB |
| Regime data | `regime.jsonl` | JSONL | 349 KB |

### Структура training_data.jsonl:

```json
{
  "timestamp": "2026-01-30T12:34:56Z",
  "symbol": "BTCUSDT",
  "signal_type": "PUMP",
  "entry_price": 84000.0,
  "exit_price": 84500.0,
  "pnl_pct": 0.6,
  "outcome": "TP_HIT",
  "features": {
    "delta_1m": 0.5,
    "delta_5m": 1.2,
    "volume_ratio": 2.3,
    "buy_dominance": 0.7,
    "rsi_14": 65,
    "atr_pct": 1.5,
    "btc_correlation": 0.8
  },
  "ai_confidence": 0.72,
  "market_regime": "BULLISH"
}
```

---

## 🚀 КОМАНДЫ ЗАПУСКА

### Полный Production Stack:

```powershell
# 1. Pricefeed (port 8100)
Start-Process python "scripts/pricefeed_bridge.py --daemon"

# 2. AutoTrader LIVE (port 8200)
Start-Process python "scripts/autotrader.py --mode LIVE --confirm --yes --position-size 20"

# 3. Position Watchdog
Start-Process python "scripts/position_watchdog.py --live"

# 4. Pump Detector (с AI)
Start-Process python "scripts/pump_detector.py --top 20"

# 5. Live Learning (обучение)
Start-Process python "scripts/live_learning.py --daemon"

# 6. Dashboard (port 8080)
Start-Process python "scripts/hope_dashboard.py --port 8080"
```

### Проверка статуса:

```powershell
# Порты
netstat -ano | Select-String ":8080|:8100|:8200"

# Процессы
Get-Process python

# Логи
Get-Content logs/autotrader.log -Tail 20
```

---

## 📈 МЕТРИКИ УСПЕХА

| Метрика | Текущее | Цель (3 дня) | Цель (неделя) |
|---------|---------|--------------|---------------|
| Win Rate | ~50% | 55% | 60% |
| R:R | 1.5:1 | 3:1 | 3:1 |
| Model Accuracy | N/A | 65% | 75% |
| Trades/Day | 0 | 10+ | 20+ |
| PnL/Day | -$0.22 | +$30 | +$50 |
| Uptime | ~80% | 95% | 99% |

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Model overfitting | HIGH | Cross-validation, holdout set |
| API rate limits | MEDIUM | Rate limiter, caching |
| False signals | HIGH | R:R >= 3:1 requirement |
| System crash | LOW | Watchdog, auto-restart |
| Data corruption | LOW | Atomic writes, backups |

---

## ✅ CHECKLIST ПЕРЕД LIVE

- [ ] Все py_compile = PASS
- [ ] Pricefeed fresh (< 30 sec)
- [ ] Models loaded
- [ ] Watchdog running
- [ ] Dashboard accessible
- [ ] Circuit Breaker reset
- [ ] R:R >= 3:1 enforced
- [ ] Position size = $20
- [ ] Max positions = 20

---

**Этот документ является основой для развертывания HOPE AI Ecosystem v2.0**
