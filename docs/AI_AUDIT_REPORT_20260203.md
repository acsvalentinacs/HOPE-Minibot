# HOPE AI AUDIT REPORT
**Date:** 2026-02-03
**Status:** PRODUCTION SYSTEM (LIVE TRADING)

---

## 1. Список всех AI модулей и их обязанности

| ID | Название | Файл | Обязанность | Артефакт |
|----|----------|------|-------------|----------|
| 1 | **Sentiment Analyzer** | `ai_gateway/modules/sentiment/` | Анализ рыночных настроений (Fear/Greed, новости, соцсети) | `sentiment.jsonl` |
| 2 | **Regime Detector** | `ai_gateway/modules/regime/` | Определение режима рынка (тренд/флэт/волатильность) | `regime.jsonl` |
| 3 | **Strategy Doctor** | `ai_gateway/modules/doctor/` | Диагностика здоровья стратегии, рекомендации | `doctor.jsonl` |
| 4 | **Anomaly Scanner** | `ai_gateway/modules/anomaly/` | Обнаружение аномалий (спайки, киты, корреляции) | `anomaly.jsonl` |
| 5 | **Self-Improver** | `ai_gateway/modules/self_improver/` | Самообучающаяся модель с A/B тестами | `self_improver.jsonl` |
| 6 | **Signal Classifier** | `ai_gateway/modules/predictor/signal_classifier.py` | ML классификация сигналов MoonBot | - |
| 7 | **Price Feed Bridge** | `ai_gateway/feeds/price_bridge.py` | Реальные цены для outcome tracking | - |
| 8 | **Outcome Tracker** | `ai_gateway/feeds/outcome_tracker.py` | MFE/MAE отслеживание исходов сигналов | `outcomes.jsonl` |

### Дополнительные AI компоненты:

| Компонент | Файл | Функция |
|-----------|------|---------|
| Eye of God V3 | `scripts/eye_of_god_v3.py` | Главный торговый движок с AI фильтрами |
| AutoTrader | `scripts/autotrader.py` | HTTP API для приёма сигналов |
| Signal Watcher | `scripts/signal_watcher.py` | Мониторинг сигналов MoonBot → AutoTrader |
| Engine Watchdog | `scripts/engine_watchdog.py` | Контроль heartbeat, Telegram алерты |

---

## 2. Подключение к /AI панели Telegram

### ✅ Подключены к /AI (через AI Gateway HTTP API):

| Модуль | Endpoint | Статус в /AI |
|--------|----------|--------------|
| Sentiment | `/status/sentiment` | 🟢🟡🔴⚪ |
| Regime | `/status/regime` | 🟢🟡🔴⚪ |
| Doctor | `/status/doctor` | 🟢🟡🔴⚪ |
| Anomaly | `/status/anomaly` | 🟢🟡🔴⚪ |
| Self-Improver | `/status/self_improver` | 🟢🟡🔴⚪ |

### ❌ НЕ подключены к /AI:

| Компонент | Причина | Нужно подключить? |
|-----------|---------|-------------------|
| Signal Classifier | Работает как часть Eye of God | Нет (внутренний) |
| Price Feed Bridge | Инфраструктурный компонент | Опционально |
| Outcome Tracker | Инфраструктурный компонент | Опционально |
| Engine Watchdog | Отдельный процесс | Да, рекомендуется |
| Signal Watcher | Отдельный процесс | Да, рекомендуется |

---

## 3. Текущие проблемы и решения

### 🔴 КРИТИЧЕСКАЯ: StatusManager несовместимость на VPS

**Проблема:** VPS имеет упрощённую версию `status_manager.py` без методов:
- `get_last_run()`
- `get_error_count()`
- `get_tooltip()`
- `mark_healthy()`, `mark_error()`, `mark_warning()`
- `enable_module()`, `disable_module()`
- `format_status_block()`, `format_detail_block()`

**Симптом:** `/AI` панель показывает ошибку "Internal Server Error" при запросе статуса.

**Решение:**
```bash
# На VPS выполнить:
cd /opt/hope/minibot

# 1. Скопировать файлы с локальной машины или использовать scp
# contracts.py и status_manager.py из ai_gateway/

# 2. Перезапустить AI Gateway
sudo systemctl restart hope-ai-gateway

# 3. Проверить
curl -s http://127.0.0.1:8100/status | jq .
```

### 🟡 СРЕДНЯЯ: AI Gateway не запускает модули автоматически

**Проблема:** Модули (sentiment, regime, etc.) disabled по умолчанию.

**Решение:**
```bash
# Включить нужные модули:
curl -X POST http://127.0.0.1:8100/modules/regime/enable
curl -X POST http://127.0.0.1:8100/modules/anomaly/enable

# Запустить модуль:
curl -X POST http://127.0.0.1:8100/modules/regime/start
```

### 🟡 СРЕДНЯЯ: ANTHROPIC_API_KEY не настроен

**Проблема:** Sentiment и Doctor используют Claude API, но ключ не настроен.

**Решение:**
```bash
# Добавить в /etc/hope/.env:
echo "ANTHROPIC_API_KEY=sk-ant-..." | sudo tee -a /etc/hope/.env
sudo systemctl restart hope-ai-gateway
```

### 🟢 НИЗКАЯ: Артефакты не генерируются

**Проблема:** Файлы `sentiment.jsonl`, `regime.jsonl` пустые или отсутствуют.

**Причина:** Модули не включены или не хватает входных данных.

**Решение:**
1. Включить модули (см. выше)
2. Подключить PriceFeed для данных свечей

---

## 4. Что нужно исправить (приоритеты)

### P0 (Критические - блокируют торговлю):

| # | Задача | Статус | Действие |
|---|--------|--------|----------|
| 1 | Синхронизировать status_manager.py на VPS | ❌ | Скопировать файл |
| 2 | Синхронизировать contracts.py на VPS | ❌ | Скопировать файл |
| 3 | Перезапустить hope-ai-gateway | ❌ | `systemctl restart` |

### P1 (Важные - влияют на AI качество):

| # | Задача | Статус | Действие |
|---|--------|--------|----------|
| 4 | Настроить ANTHROPIC_API_KEY | ❌ | Добавить в /etc/hope/.env |
| 5 | Включить Regime Detector | ❌ | POST /modules/regime/enable |
| 6 | Включить Anomaly Scanner | ❌ | POST /modules/anomaly/enable |
| 7 | Подключить PriceFeed | ❌ | Нужен WebSocket к Binance |

### P2 (Улучшения):

| # | Задача | Действие |
|---|--------|----------|
| 8 | Добавить Engine Watchdog в /AI | Расширить tg_bot_simple.py |
| 9 | Добавить Signal Watcher в /AI | Расширить tg_bot_simple.py |
| 10 | Настроить автозапуск модулей | Изменить scheduler.py |

---

## 5. Архитектурные мысли (honest assessment)

### Что хорошо:

1. **Fail-closed дизайн** - система останавливается при ошибках, не продолжает рискованно
2. **JSONL артефакты** - воспроизводимый обмен между модулями
3. **StatusManager** - единая точка отслеживания здоровья
4. **Модульность** - можно включать/отключать отдельные AI
5. **PriceFeed Bridge** - реальные цены для outcome tracking

### Что требует внимания:

1. **VPS/Local расхождение** - код на VPS отстаёт от локального
   - **Решение:** CI/CD или регулярная синхронизация

2. **Зависимость от внешних API** - Claude (Anthropic), Binance
   - **Решение:** Fallback режимы, кэширование

3. **Отсутствие health мониторинга** - нет единого дашборда
   - **Решение:** Добавить /AI → /HEALTH с агрегатом всех сервисов

4. **Manual module enable** - модули не стартуют автоматически
   - **Решение:** Config файл с enabled_by_default

5. **Нет A/B тестов в production** - Self-Improver не активен
   - **Решение:** Включить после baseline валидации

### Рекомендации:

1. **Немедленно:** Синхронизировать код VPS с локальным
2. **Краткосрочно:** Настроить автозапуск модулей regime + anomaly
3. **Среднесрочно:** Добавить централизованный /HEALTH endpoint
4. **Долгосрочно:** CI/CD для автоматического деплоя на VPS

---

## Команды для исправления на VPS

```bash
# SSH на VPS
ssh -i ~/.ssh/id_ed25519_hope hope@46.62.232.161

# 1. Проверить текущий статус
sudo systemctl status hope-ai-gateway
curl -s http://127.0.0.1:8100/health

# 2. Проверить логи
sudo journalctl -u hope-ai-gateway -n 50

# 3. После копирования файлов - рестарт
sudo systemctl restart hope-ai-gateway

# 4. Включить модули
curl -X POST http://127.0.0.1:8100/modules/regime/enable
curl -X POST http://127.0.0.1:8100/modules/regime/start
curl -X POST http://127.0.0.1:8100/modules/anomaly/enable
curl -X POST http://127.0.0.1:8100/modules/anomaly/start

# 5. Проверить /AI панель
curl -s http://127.0.0.1:8100/status | jq .
```

---

## Файлы для копирования на VPS

Из локальной папки `C:\Users\kirillDev\Desktop\TradingBot\minibot\`:

```
ai_gateway/contracts.py       → /opt/hope/minibot/ai_gateway/
ai_gateway/status_manager.py  → /opt/hope/minibot/ai_gateway/
```

---

**Report generated by:** Claude (opus-4.5)
**Generated at:** 2026-02-03T17:05:00Z
