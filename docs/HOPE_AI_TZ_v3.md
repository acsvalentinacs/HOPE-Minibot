# HOPE AI ECOSYSTEM v3.0 - ТЕХНИЧЕСКОЕ ЗАДАНИЕ

**Версия:** 3.0
**Дата:** 2026-01-31
**Статус:** PRODUCTION READY

---

## 1. ТЕКУЩЕЕ СОСТОЯНИЕ

### 1.1 Работающие компоненты

| Компонент | Файл | Порт | Статус | Функция |
|-----------|------|------|--------|---------|
| AutoTrader | `scripts/autotrader.py` | 8200 | ✅ ACTIVE | Исполнение ордеров |
| Pump Detector | `scripts/pump_detector.py` | - | ✅ ACTIVE | WebSocket мониторинг |
| Position Watchdog | `scripts/position_watchdog.py` | - | ✅ ACTIVE | Защита позиций |
| Pricefeed Bridge | `scripts/pricefeed_bridge.py` | - | ✅ ACTIVE | Цены 34 символов |
| Eye of God V3 | `scripts/eye_of_god_v3.py` | - | ⚠️ EMBEDDED | Встроен в AutoTrader |
| Dashboard | `dashboard/dashboard_server.py` | 8080 | ✅ ACTIVE | UI мониторинг |

### 1.2 Обученные модели

| Модель | Файл | Размер | Назначение |
|--------|------|--------|------------|
| Signal Classifier | `state/ai/signal_classifier.joblib` | 74 KB | Классификация сигналов |
| Anomaly Detector | `state/ai/anomaly_model.joblib` | 1.3 MB | Детекция аномалий |

### 1.3 НЕ работающие компоненты

| Компонент | Проблема | Критичность |
|-----------|----------|-------------|
| AI Predictor V2 | asyncio crash | 🔴 HIGH |
| Live Learning | Не запущен | 🔴 HIGH |
| Signal Aggregator | Нет daemon mode | 🟡 MEDIUM |
| Adaptive Target AI | Не интегрирован | 🟡 MEDIUM |

---

## 2. ЦЕЛЕВАЯ АРХИТЕКТУРА

```
┌─────────────────────────────────────────────────────────────────┐
│                     HOPE AI ECOSYSTEM v3.0                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  LAYER 1: DATA SOURCES                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                        │
│  │ Binance  │ │ MoonBot  │ │TradingView│                        │
│  │WebSocket │ │ Telegram │ │  Alerts   │                        │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘                        │
│       └────────────┼────────────┘                               │
│                    ▼                                             │
│  LAYER 2: SIGNAL PROCESSING                                     │
│  ┌─────────────────────────────────────────────┐               │
│  │              pump_detector.py                │               │
│  │  • WebSocket connection to Binance           │               │
│  │  • buys/s, delta%, volume metrics           │               │
│  │  • Forward signals to AutoTrader            │               │
│  └────────────────────┬────────────────────────┘               │
│                       ▼                                          │
│  LAYER 3: AI BRAIN                                              │
│  ┌─────────────────────────────────────────────┐               │
│  │ ┌─────────────┐ ┌─────────────┐ ┌─────────┐ │               │
│  │ │ Predictor   │ │ Aggregator  │ │Adaptive │ │               │
│  │ │    V2       │ │             │ │Target AI│ │               │
│  │ │ confidence  │ │ multi-source│ │ TP/SL   │ │               │
│  │ │   0-100%    │ │   scoring   │ │dynamics │ │               │
│  │ └──────┬──────┘ └──────┬──────┘ └────┬────┘ │               │
│  │        └───────────────┼─────────────┘      │               │
│  │                        ▼                     │               │
│  │              ┌─────────────────┐            │               │
│  │              │  Eye of God V3  │            │               │
│  │              │ Two-Chamber     │            │               │
│  │              │ Decision System │            │               │
│  │              └────────┬────────┘            │               │
│  └───────────────────────┼─────────────────────┘               │
│                          ▼                                       │
│  LAYER 4: EXECUTION                                             │
│  ┌─────────────────────────────────────────────┐               │
│  │              autotrader.py                   │               │
│  │  • Binance API execution                    │               │
│  │  • OCO orders (TP + SL)                     │               │
│  │  • Position management                      │               │
│  └────────────────────┬────────────────────────┘               │
│                       ▼                                          │
│  LAYER 5: LEARNING                                              │
│  ┌─────────────────────────────────────────────┐               │
│  │ ┌─────────────┐ ┌─────────────┐             │               │
│  │ │Live Learning│ │ Eye Trainer │             │               │
│  │ │ outcomes →  │ │ retrain     │             │               │
│  │ │ update      │ │ every 100   │             │               │
│  │ └─────────────┘ └─────────────┘             │               │
│  └─────────────────────────────────────────────┘               │
│                                                                  │
│  LAYER 6: MONITORING                                            │
│  ┌─────────────────────────────────────────────┐               │
│  │ Dashboard :8080 │ Watchdog │ Circuit Breaker│               │
│  └─────────────────────────────────────────────┘               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. ПОТОКИ ДАННЫХ

### 3.1 Signal Flow

```
Binance WebSocket
       │
       ▼ trades stream
┌──────────────────┐
│  pump_detector   │──▶ Calculates: buys/s, delta%, volume
└────────┬─────────┘
         │ HTTP POST http://127.0.0.1:8200/signal
         ▼
┌──────────────────┐
│   autotrader     │──▶ Eye of God V3 decision
└────────┬─────────┘
         │ BUY/SKIP
         ▼
┌──────────────────┐
│  Binance API     │──▶ MARKET order + OCO (TP/SL)
└──────────────────┘
```

### 3.2 Data Files

| Файл | Назначение | Обновление |
|------|------------|------------|
| `state/ai/pricefeed.json` | Текущие цены | Каждые 5 сек |
| `state/ai/signals/raw.jsonl` | Сырые сигналы | При поступлении |
| `state/ai/decisions.jsonl` | Решения Eye of God | При каждом сигнале |
| `state/ai/autotrader/trades.jsonl` | Исполненные сделки | При исполнении |
| `state/ai/outcomes/history.jsonl` | Результаты (PnL) | При закрытии |
| `state/ai/training_data.jsonl` | Данные обучения | После каждой сделки |

---

## 4. ПЛАН УСТАНОВКИ НЕДОСТАЮЩИХ КОМПОНЕНТОВ

### ФАЗА 1: Исправление AI Predictor V2 (30 мин)

**Проблема:** asyncio event loop crash

**Решение:** Создать новый entry point с правильной структурой

```python
# scripts/ai_predictor_daemon.py
import asyncio
from ai_predictor_v2 import AIPredictor

async def main():
    predictor = AIPredictor()
    await predictor.run_daemon()

if __name__ == "__main__":
    asyncio.run(main())
```

### ФАЗА 2: Интеграция Live Learning (1 час)

**Задача:** Автоматическое обучение на результатах сделок

**Компоненты:**
1. `live_learning.py` - сборщик outcomes
2. `eye_trainer.py` - переобучение моделей
3. Hot-reload в AutoTrader

**Интеграция:**
```python
# В autotrader.py после закрытия позиции:
from scripts.live_learning import record_outcome

async def on_position_close(position, result):
    await record_outcome({
        "symbol": position.symbol,
        "entry": position.entry_price,
        "exit": result.exit_price,
        "pnl_pct": result.pnl_pct,
        "reason": result.reason,  # TP_HIT, SL_HIT, TIMEOUT
        "signal_confidence": position.signal_confidence,
        "timestamp": datetime.utcnow().isoformat()
    })
```

### ФАЗА 3: Signal Aggregator Daemon (30 мин)

**Задача:** Постоянная агрегация сигналов из разных источников

```python
# scripts/signal_aggregator.py --daemon
async def daemon_loop():
    while True:
        signals = await collect_signals()
        score = await aggregate_and_score(signals)
        await publish_to_autotrader(score)
        await asyncio.sleep(5)
```

### ФАЗА 4: Dashboard с графиками (1 час)

**Новые эндпоинты:**
- `GET /api/stats` - Win Rate, Total PnL, Trades
- `GET /api/chart/pnl` - PnL over time
- `GET /api/chart/winrate` - Win rate trend
- `GET /api/chart/confidence` - AI confidence distribution
- `GET /api/chart/model` - Model performance metrics

---

## 5. ПОСЛЕДОВАТЕЛЬНОСТЬ ЗАПУСКА

```bash
# 1. Pricefeed Bridge (данные цен)
python scripts/pricefeed_bridge.py --daemon

# 2. AI Predictor (предсказания)
python scripts/ai_predictor_daemon.py

# 3. AutoTrader + Eye of God V3 (исполнение)
python scripts/autotrader.py --mode LIVE --confirm --yes

# 4. Position Watchdog (защита)
python scripts/position_watchdog.py --live

# 5. Pump Detector (сигналы)
python scripts/pump_detector.py --top 20

# 6. Live Learning (обучение)
python scripts/live_learning.py --daemon

# 7. Dashboard (мониторинг)
python dashboard/dashboard_server.py --port 8080
```

---

## 6. ПРОЦЕСС ОБУЧЕНИЯ

### 6.1 Начальное обучение

```bash
# Шаг 1: Собрать исторические данные
python scripts/sync_training_data.py

# Шаг 2: Обучить классификатор сигналов
python scripts/eye_trainer.py --train \
  --data state/ai/training_data.jsonl \
  --output state/ai/signal_classifier.joblib

# Шаг 3: Обучить детектор аномалий
python scripts/eye_trainer.py --train-anomaly \
  --data state/ai/anomaly.jsonl \
  --output state/ai/anomaly_model.joblib

# Шаг 4: Валидация
python scripts/eye_trainer.py --validate
```

### 6.2 Онлайн обучение (Live Learning)

```
Trade Close Event
       │
       ▼
┌──────────────────┐
│  record_outcome  │──▶ state/ai/outcomes/history.jsonl
└────────┬─────────┘
         │ check: trades >= 100?
         ▼
┌──────────────────┐
│  trigger_retrain │──▶ eye_trainer.py --incremental
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  hot_reload      │──▶ AutoTrader reloads models
└──────────────────┘
```

### 6.3 Данные для обучения

| Источник | Поля | Использование |
|----------|------|---------------|
| Pump Detector | buys_per_sec, delta_pct, volume | Features |
| Eye of God | confidence, mode, factors | Features |
| Trade Results | pnl_pct, reason | Labels |
| Market Data | price, volatility, regime | Context |

---

## 7. МЕТРИКИ КАЧЕСТВА

| Метрика | Целевое значение | Текущее |
|---------|------------------|---------|
| Win Rate | > 55% | TBD |
| R:R Ratio | >= 3:1 | 3:1 |
| Max Drawdown | < 5% daily | TBD |
| Model Accuracy | > 65% | TBD |
| Signal Latency | < 100ms | ~50ms |
| Uptime | > 99% | TBD |

---

## 8. МОНИТОРИНГ (Dashboard :8080)

### 8.1 Метрики в реальном времени

- **Win Rate** - процент прибыльных сделок
- **Total PnL** - суммарная прибыль/убыток
- **Trades Today** - количество сделок за день
- **Model Accuracy** - точность AI модели

### 8.2 Графики

1. **PnL Over Time** - кривая доходности
2. **Win Rate Trend** - тренд win rate
3. **AI Confidence Distribution** - распределение уверенности
4. **Model Performance** - accuracy, precision, recall, F1

### 8.3 Кнопки управления

- **[REFRESH ALL]** - обновить все данные
- **[START STACK]** - запустить все компоненты
- **[STOP ALL]** - остановить все
- **[RETRAIN MODEL]** - принудительное переобучение

---

## 9. FAIL-CLOSED ПРИНЦИПЫ

```
✅ Сигнал валидный      → Продолжить
❌ Сигнал невалидный    → STOP (не пропускать)

✅ Цена свежая (< 30s)  → Продолжить
❌ Цена устаревшая      → STOP (не торговать)

✅ R:R >= 3:1           → Продолжить
❌ R:R < 3:1            → SKIP (не открывать)

✅ Liquidity > $5M      → Продолжить
❌ Liquidity < $5M      → SKIP (слишком рискованно)
```

---

**Этот документ является SSoT для AI компонентов HOPE.**
