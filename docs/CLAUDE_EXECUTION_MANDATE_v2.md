# CLAUDE EXECUTION MANDATE v2.0

**Version:** 2026-01-31
**Status:** ACTIVE | MANDATORY
**Scope:** ALL Claude sessions working on HOPE

---

## 🔴 ГЛАВНЫЙ ПРИНЦИП

```
НЕ "МОГУ" — СДЕЛАНО!
НЕ "НАПИСАНО" — ИНТЕГРИРОВАНО!
НЕ "СОЗДАНО" — РАБОТАЕТ В PRODUCTION!
```

---

## ⚡ ЦИКЛ ВЫПОЛНЕНИЯ

```
1. КОД        → Написать решение
2. ТЕСТ       → py_compile + import test
3. PASS       → Все проверки зелёные
4. ИНТЕГРАЦИЯ → Файл подключен к системе
5. COMMIT     → git add + git commit
6. VERIFY     → Проверить что работает
```

**Нет пропусков. Нет "потом". Нет "можно добавить".**

---

## 🛡️ FAIL-CLOSED

```python
if сомнение:
    return FAIL  # Исправь сейчас

if ошибка:
    return FAIL  # Исправь сейчас

if не_уверен:
    return FAIL  # Проверь и исправь
```

---

## 🚫 ЗАПРЕЩЕНО

| Запрещено | Правильно |
|-----------|-----------|
| "Предлагаю..." | ВЫПОЛНИ |
| "Можно добавить..." | ДОБАВЬ |
| "Надо бы..." | СДЕЛАЙ |
| "Могу сделать..." | СДЕЛАНО |
| Файл без теста | НЕ СУЩЕСТВУЕТ |
| Код без коммита | НЕ СДЕЛАН |
| Модуль без интеграции | НЕ ГОТОВ |

---

## ✅ ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ

```bash
# 1. Синтаксис
python -m py_compile <file.py>

# 2. Импорты
python -c "from <module> import *; print('OK')"

# 3. Соседние файлы
grep -rn "from <module> import" scripts/*.py

# 4. Интеграция
# Проверить что модуль вызывается из основного flow

# 5. Коммит
git add <files>
git commit -m "type(scope): description"

# 6. Verify
curl http://localhost:8200/status  # или другой endpoint
```

---

## 📦 ФОРМУЛА УСПЕХА

```
ЗАДАЧА → КОД → ТЕСТ → PASS → ИНТЕГРАЦИЯ → COMMIT → VERIFY → DONE
```

**НЕТ ОПРАВДАНИЙ. НЕТ ПОЛУМЕР. ТОЛЬКО РЕЗУЛЬТАТ.**

---

## 🎯 КОНТЕКСТ

HOPE = живой торговый бот на Binance MAINNET.

- Каждый файл должен РАБОТАТЬ в production
- Каждое изменение влияет на РЕАЛЬНЫЕ ДЕНЬГИ
- Это НАШ проект, мы за него ОТВЕЧАЕМ
- Нельзя бросить в нерабочем состоянии

---

## 📋 TASK COMPLETION FORMAT

```markdown
=== TASK COMPLETION ===

Task: [описание]
Result: PASS / FAIL

✅ СДЕЛАНО:
- [конкретный результат]

❌ ОШИБКИ:
- [если есть]

COMMITS:
- abc1234 type(scope): description

VERIFICATION:
- py_compile: PASS
- import test: PASS
- integration: PASS
- endpoint check: PASS
```

---

**Этот документ — ЗАКОН для всех сессий Claude при работе с HOPE.**
